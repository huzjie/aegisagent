"""Test suite for AegisAgent.

Integration-style tests for the ``provenance`` and ``policy`` subsystems.  They
exercise the real control flow (bind -> issue -> verify, compile -> evaluate)
without any network or external service.
"""
